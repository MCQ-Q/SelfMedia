# AI 视频台本生成系统 使用说明

## 一、启动与停止

### 启动开发服务器

```bash
cd D:\SelfMedia
pnpm dev
```

启动成功后访问 **http://localhost:3000**，会自动跳转到项目列表页。

### 停止服务器

在终端里按 `Ctrl + C`。

### 端口被占用了？

如果启动时提示端口 3000 被占用：

```bash
# PowerShell 或 CMD 里执行
taskkill /F /IM node.exe

# 然后重新启动
pnpm dev
```

### 改了配置要重启吗？

| 改了什么 | 要不要重启 |
|---|---|
| `.env` 文件 | **必须重启** |
| `prisma/schema.prisma` | 改完先跑 `npx prisma db push`，再重启 |
| 其他 `.ts` / `.tsx` 源码 | **不用重启**，Next.js 会自动热更新 |

---

## 二、配置文件说明

所有配置文件都在项目根目录 `D:\SelfMedia\` 下。

### `.env` —— 大模型和数据库配置

这是你日常最可能改的文件。

```bash
# DeepSeek API 密钥
LLM_API_KEY=sk-8dd91f3df0244ac6afd20db8b39ae297

# API 地址（OpenAI 兼容接口均可，不限于 DeepSeek）
LLM_BASE_URL=https://api.deepseek.com/v1

# 选题生成用的模型（建议用快的，省成本）
LLM_TOPIC_MODEL=deepseek-v4-flash

# 台本生成用的模型（建议用强的，保证质量）
LLM_SCRIPT_MODEL=deepseek-v4-pro

# 请求超时时间，单位毫秒（台本生成较慢，可酌情调大）
LLM_REQUEST_TIMEOUT_MS=180000

# 数据库文件路径（本地 SQLite，不需要装数据库服务）
DATABASE_URL="file:./dev.db"
```

每个变量是干什么的：

| 变量名 | 用途 | 多久改一次 |
|---|---|---|
| `LLM_API_KEY` | API 密钥，换服务商时改 | 偶尔 |
| `LLM_BASE_URL` | 接口地址，DeepSeek / OpenAI / 其他兼容服务 | 偶尔 |
| `LLM_TOPIC_MODEL` | 选题 Agent 用的模型，快模型即可 | 按需 |
| `LLM_SCRIPT_MODEL` | 台本 Agent 用的模型，建议用强模型 | 按需 |
| `LLM_REQUEST_TIMEOUT_MS` | 超时时间，台本超过 180 秒可改大，如 `300000` | 很少 |
| `DATABASE_URL` | 数据库文件位置 | 基本不改 |

### `prisma/schema.prisma` —— 数据库表结构

定义了 5 张表：项目、素材、选题、参考内容、台本版本。

改了这个文件之后，执行：

```bash
npx prisma db push
```

### Agent 的提示词在哪里？

选题和台本生成的系统提示词、调用参数都在同一个文件里：

**`src/lib/workflow-runner.ts`**

| 内容 | 对应常量或方法 |
|---|---|
| 选题 Agent 的提示词 | `TOPIC_SYSTEM_PROMPT` |
| 台本 Agent 的提示词 | `SCRIPT_SYSTEM_PROMPT` |
| 生成温度等参数 | `runTopicGeneration()` 和 `runScriptGeneration()` 方法 |

另外，台本生成后的质量校验规则在：

**`src/lib/schemas.ts`** → `validateScriptContent` 函数

---

## 三、两个 Agent 分别干什么

### 选题 Agent

- **输入**：你贴的原始素材 + 平台 + 视频类型 + 目标时长
- **输出**：3 到 5 个候选选题，每个选题包含标题、目标受众、核心冲突、情绪基调、推荐分数、推荐理由
- **使用模型**：由 `LLM_TOPIC_MODEL` 指定，建议用 flash 等快模型
- **生成温度**：0.5

### 台本生成 Agent

- **输入**：原始素材 + 你选中的选题 +（可选）参考文案 + 目标时长
- **输出**：一份完整的结构化台本，包含若干段落，每段都有：
  - 台词（口播要说的话）
  - 字幕文字
  - 画面描述 + AI 生图提示词
  - 情绪类型和强度（1-10）
  - BGM 风格建议
  - 导演备注（剪辑节奏、停顿、重音等提示）
- **使用模型**：由 `LLM_SCRIPT_MODEL` 指定，建议用 pro 等强模型
- **生成温度**：0.6

### 自动校验

台本生成后系统会自动检查：
- JSON 格式对不对
- 段落数够不够（至少 3 段）
- 第一段是不是从 0 秒开始
- 段落之间有没有时间重叠
- 台词字数跟目标时长是否匹配（中文每秒约 4-6 字）
- 最后一段的结束时间是否接近目标时长（允许 10% 偏差）

如果校验没通过，系统会自动让大模型再修一次（repair retry）。修了还是不行才会报错。

---

## 四、完整使用流程

### 第一步：创建项目

打开 `http://localhost:3000/projects/new`

填几个东西：
- **项目标题**：给你的视频起个名字
- **目标平台**：抖音 / 小红书 / B站 / 视频号
- **视频类型**：口播 / 故事 / 知识 / 观点
- **目标时长**：15 到 180 秒，默认 60 秒
- **原始素材**：把你的聊天记录、灵感笔记、已有文案贴进去

点「创建项目」，会自动跳转到工作台。

### 第二步：生成选题

在工作台页面（`/projects/[id]/workbench`）点「生成选题」。

系统会根据你的素材生成 3-5 个候选选题，每个都有评分和推荐理由。看到合适的点一下就能选中。

如果想重新生成，点「重新生成」按钮，旧的未选中选题会被覆盖。

### 第三步：添加参考（可以跳过）

参考内容是选填的，作用是让 AI 理解你喜欢的风格和结构。

如果要加，点「添加参考」展开输入区，贴参考视频的文案或字幕就行。AI 只参考风格和结构，不会抄袭内容。

### 第四步：生成台本

选好选题后，点「生成台本」按钮。通常 30-90 秒能出结果，具体看模型和时长。

生成完会提示成功，点「查看已有台本」进入台本页。

### 第五步：编辑和导出

台本页面按时间轴分段展示，每段都能看到台词、字幕、画面、情绪、BGM、导演备注。

你可以：
- **查看**：直接浏览每个段落的完整信息
- **编辑**：点右上角「编辑」按钮，可以修改台词、字幕、画面描述、情绪、导演备注等，改完点「保存」
- **导出**：点「导出 Markdown」，浏览器会下载一个 `.md` 文件，可以直接给剪辑师或自己用

---

## 五、项目目录速查

```
D:\SelfMedia\
├── .env                          # ★ 核心配置文件
├── .npmrc                        # 包管理器配置，不用管
├── prisma/
│   ├── schema.prisma             # 数据库表结构定义
│   └── dev.db                    # SQLite 数据库文件（你的数据在这里）
├── src/
│   ├── app/
│   │   ├── api/projects/...      # 后端接口（11 个 API 路由）
│   │   ├── projects/             # 前端页面（4 个）
│   │   └── layout.tsx            # 全局页面布局
│   ├── components/ui/            # 通用 UI 组件（按钮、卡片等）
│   └── lib/
│       ├── workflow-runner.ts    # ★ Agent 提示词和生成流程
│       ├── schemas.ts            # ★ 数据结构定义和校验规则
│       ├── llm-client.ts         # 大模型调用封装
│       ├── markdown-exporter.ts  # Markdown 导出渲染
│       ├── prisma.ts             # 数据库连接
│       ├── errors.ts             # 错误类型
│       └── utils.ts              # 工具函数
├── package.json                  # 依赖包声明
├── tsconfig.json                 # TypeScript 配置
├── 使用说明.md                    # 就是你现在看的这个文件
└── video-script-system-design-v5.md   # 系统设计文档
```

打 ★ 的是你最可能需要关注的文件。

---

## 六、常见问题

### 怎么看数据库里的数据？

```bash
npx prisma studio
```

会自动打开一个网页（`http://localhost:5555`），可以在里面浏览和编辑所有表的数据。

### 怎么清空数据库重新来过？

```bash
rm prisma/dev.db          # 删掉数据库文件
npx prisma db push        # 重建空数据库
```

### 生成台本超时了怎么办？

把 `.env` 里的超时时间改大：

```bash
LLM_REQUEST_TIMEOUT_MS=300000   # 改成 5 分钟
```

改完重启服务。

### 想换别的模型供应商？

只要是 OpenAI 兼容的接口（`/v1/chat/completions` 格式），不用改代码，只改 `.env` 就行：

```bash
# 比如换成 OpenAI
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=sk-your-openai-key
LLM_TOPIC_MODEL=gpt-4o-mini
LLM_SCRIPT_MODEL=gpt-4o
```

### npm / pnpm 安装包太慢？

设置国内镜像：

```bash
pnpm config set registry https://registry.npmmirror.com
```

### 怎么调整生成风格？

编辑 `src/lib/workflow-runner.ts`：
- 修改 `TOPIC_SYSTEM_PROMPT` 改变选题思路
- 修改 `SCRIPT_SYSTEM_PROMPT` 改变台本风格
- 修改 `temperature` 值，越高越随机，越低越稳定

---

## 七、快捷命令速查

```bash
pnpm dev                        # 启动开发服务器
npx prisma studio               # 打开数据库管理网页
npx prisma db push              # 同步数据库结构（改 schema 后执行）
taskkill /F /IM node.exe        # 强制停掉所有 Node 进程（Windows）
```
